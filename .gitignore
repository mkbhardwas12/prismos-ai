# Dependencies
node_modules/

# Build output
dist/
build/

# Rust / Tauri
target/
src-tauri/gen/
src-tauri/target/

# Environment
.env
.env.*
# Only reviewed, placeholder-only examples belong in public source.
!.env.example
!.env.sample
!.env.template

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
Desktop.ini

# Demo build artifacts (intermediate; regenerate via scripts/build-demo.sh)
docs/media/_overlays/
docs/media/_brain_wrapped/
docs/media/_stream/
docs/screenshots/live/

# Private / legal (do not sync)
*_EVIDENCE.md
*_evidence*.md

# ─────────────────────────────────────────────────────────────────────────────
# PRIVATE knowledge — keep on-device, NEVER push to the public repo.
# Personal project knowledge can be ingested locally, but must not be committed.
# ─────────────────────────────────────────────────────────────────────────────
knowledge/
# Public, reviewed reference pack only. Personal knowledge/ remains ignored.
!/resources/knowledge/
/resources/knowledge/*
!/resources/knowledge/reliable-local-assistant/
/resources/knowledge/reliable-local-assistant/*
!/resources/knowledge/reliable-local-assistant/*.md
!/resources/knowledge/reliable-local-assistant/manifest.json
PrismDocs/
.prismos-private-terms

# Local AI-agent configuration and project guides may include private project
# details, local paths or credentials. Never push them to the public repo.
CLAUDE.md
.claude/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# ─────────────────────────────────────────────────────────────────────────────
# PERSONAL "BRAIN" / MEMORY — NEVER COMMIT (this repo is PUBLIC)
# Knowledge graph, conversation history, feedback, cognitive profile, any export.
# ─────────────────────────────────────────────────────────────────────────────
# Database (local user data — the brain)
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.sqlite-wal
*.sqlite-shm
*.sqlite3-wal
*.sqlite3-shm
# App data directory (knowledge graph, profile, audit log) if ever copied into repo
**/com.prismos.app/
# Brain / memory exports & dumps
brain_export*.json
brain_wrapped*.json
spectrum_export*.json
cognitive_profile*.json
conversation*.json
memory_dump*.json
# Actual graph/handoff filenames (encrypted exports are still PRIVATE).
*.prismos
*.prismos-sync
prismos-handoff.state

# Self-improvement flywheel — harvested PERSONAL Q&A + fine-tuned weights
scripts/flywheel/data/
scripts/flywheel/adapters/
scripts/flywheel/fused/
scripts/flywheel/holdout.jsonl
*.gguf
*.safetensors
*.npz
adapters/
fused/

# Logs
*.log
npm-debug.log*

# TypeScript
*.tsbuildinfo

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local scratch: stale git locks + repo-history bundles. NEVER push — a bundle
# carries full history, including anything scrubbed from the public tree.
_to_delete/
*.bundle
